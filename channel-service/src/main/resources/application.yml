server:
  port: 8086

spring:
  application:
    name: channel-service
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/chat_channel_db?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    username: root
    password: Nn3832143
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
